apiVersion: 1
datasources:
  - name: MongoDB
    type: grafana-mongodb-datasource
    access: proxy
    url: http://questionnaires-mongodb_proxy:3333
    basicAuth: false
    editable: true
    enabled: true
    jsonData:
      mongodb_url: ${MONGODB_URL}
      mongodb_db: 2023-thanksgiving-day
